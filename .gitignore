# Dependencies + build output
node_modules
dist
out
*.vsix
*.tsbuildinfo

# Test / coverage outputs
.vscode-test
coverage

# OS junk
.DS_Store

# Logs
*.log
npm-debug.log*

# Per-developer workspace overrides (binary paths, etc).
.vscode/settings.json

# Contributor-private assistant instructions (Claude Code).
CLAUDE.md

# Secrets — never accidentally commit a .env
.env
.env.*
!.env.example
